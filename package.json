{
  "name": "slacknews",
  "version": "1.0.0",
  "description": "SlackNews - 競合企業ニュース自動収集・分析システム",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:functions\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:functions": "npm run dev --workspace=apps/functions",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/functions",
    "deploy": "npm run deploy --workspace=apps/functions",
    "install:all": "npm install && npm install --workspace=apps/web && npm install --workspace=apps/functions"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
